language: node_js

sudo: required

node_js:
- v10.8.0

cache:
    directories:
    - node_modules

notifications:
    email:
        recipients:
        - guilherme.moraes@outlook.com
        on_success: never
        on_failure: always

services:
- rabbitmq

before_install:
- npm i enqueuer --no-save

script:
- npm run lint
- npm run build
- nqr -a examples/amqp.yml -l enqueuer-plugin-amqp
